export const searchUrls={
    // search_hotels:(query:string)=>`/hotels/searchDestination?${query}`,
     search_hotels:(query:string)=>`/hotels/searchHotels?${query}`,

    attractions_search:(query:string)=>`/attraction/searchAttractions?${query}`,
    flights_search:(query:string)=>`/flights/searchFlights?${query}`,
}